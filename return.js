// Function to multiply a number by 10
function tenTimes(num) {
    return num * 10;
}

// Function to cut a number in half
function halfOf(number) {
    const half = number / 2;
    return half;
}

tenTimes(5);

const output = tenTimes(5);
console.log('Multiplication output:', output);

const bigValue = tenTimes(99);
